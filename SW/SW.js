let cacheManager=(()=>{let ael=(t,f)=>self.addEventListener(t,f);let time=r=>{let t=r.map(v=>parseInt(v)),c=info.build;t.shift();var d=t[0]-c[0];if (!d) d=t[1]-c[1];if (!d) d=t[2]-c[2];return Math.sign(d);};let cacheList=[];var active=false;let activator=()=>{ael("install",e=>{e.waitUntil((async ()=>{let l=await caches.keys();let save=cacheList.map(()=>true);l.map(n=>{cacheList.forEach((info,i)=>{let r=n.match(new RegExp(`${info.name}-build([0-9]+).([0-9]+).([0-9]+)`));if (r) {let d=time(r);if (d==-1) return caches.delete(n);if (d==1) save[i]=false;}});});await Promise.all(cacheList.map((info,i)=>{if (save[i]) return caches.open(`${info.name}-build${info.build.join(".")}`).then(c=>c.addAll(info.list));}));})());});ael("fetch",e=>{e.respondWith((async ()=>{var res=await caches.match(e.request);if (res) return res;let req = e.request.clone();res=await fetch(req);return res;})());});active=true;};return info=>{if (!active) activator();cacheList.push(info);};})();